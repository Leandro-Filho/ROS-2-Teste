para iniciar o ambiente virtual do Ros 2, coloque esse seguinte código:

```bash
micromamba activate /home/bebaran/micromamba/envs/ros_env
```

depois para rodar o ROS 2 de fato, use:

```bash
rviz2
```

para rodar o TurtleSim, use:

```bash
ros2 run turtlesim turtlesim_node
```